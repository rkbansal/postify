{
  "name": "postify",
  "version": "1.0.0",
  "type": "module",
  "description": "URL to Social Media Post Generator",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter server dev\" \"pnpm --filter client dev\"",
    "dev:client": "pnpm --filter client dev",
    "dev:server": "pnpm --filter server dev",
    "build": "pnpm --filter client build",
    "install:all": "pnpm install && pnpm --filter client install && pnpm --filter server install",
    "clean": "pnpm --filter client clean && pnpm --filter server clean"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "social-media",
    "content-generation",
    "openai",
    "react",
    "express"
  ],
  "author": "",
  "license": "MIT"
}
